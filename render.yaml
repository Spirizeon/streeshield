services:
  - type: web
    name: backend
    env: docker
    buildCommand: docker build -t backend -f backend/Dockerfile .
    startCommand: uvicorn main:app --host 0.0.0.0
    envVars:
      - key: MONGODB_URI
        value: mongodb://mongodb:27017
    dockerContext: .
    dockerfilePath: backend/Dockerfile

  - type: web
    name: frontend
    env: node
    buildCommand: cd frontend && npm install
    envVars:
      - key: PORT
        value: 5173

  - type: pserv
    name: mongodb
    env: docker
    buildCommand: docker build -t mongodb -f frontend/Dockerfile . 
    disk:
      name: mongodb_data
      mountPath: /data/db
    envVars:
      - key: MONGO_INITDB_ROOT_USERNAME
        value: root
      - key: MONGO_INITDB_ROOT_PASSWORD
        generateValue: true
